allprojects {
    apply plugin: 'idea'
    apply plugin: 'java'
    apply plugin: 'groovy'
    group = 'com.aakn.workstore'

    idea {
        module {
            downloadSources = true
        }
    }
}

task createWrapper(type: Wrapper) {
    gradleVersion = '3.4.1'
}
